import styled from "styled-components";

export const Button = styled.button`
  display: flex;
  align-items: center;
  justify-content: center;
  column-gap: .5rem;

  cursor: pointer;
  color: inherit;
  border: none;
  background: none;
`;
